namespace TodoApp.Domain.Entities
{
    /// <summary>
    /// Represents allowed states for a Todo item.
    /// </summary>
    public enum TodoStatus
    {
        Pending,
        InProgress,
        Completed
    }
}